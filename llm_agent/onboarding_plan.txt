Phase 0: Map the Real MCDC API (2 days)
Task 0.1: Reverse-Engineer Core Functions from Regression Tests
Subtask 0.1.1: Clone MCDC/data_refactor locally.
Test: git clone -b data_refactor https://github.com/CEMeNT-PSAAP/MCDC.git succeeds.
Subtask 0.1.2: Scrape 5–10 input.py files from test/regression/ and extract all calls to mcdc.material(), mcdc.surface(), mcdc.cell(), mcdc.source(), mcdc.tally(), mcdc.setting(), mcdc.run().
Test: Create mcdc_api_summary.md listing each function with:
Markdown
Copy
Code
Preview
mcdc.material(name: str, composition: dict)
  - Example: mcdc.material('water', {'H': 2.0, 'O': 1.0})
  - Appears in: test/regression/slab/input.py, sphere/input.py, ...
Deliverable: mcdc_api_summary.md with real signatures from your codebase.
Task 0.2: Build Minimal RAG Index from Examples
Subtask 0.2.1: For each scraped input.py, copy it into onboarding/examples/ and prepend a 5-line comment block:
Python
Copy
# Example: Water sphere with isotropic source
# Concepts: material, surface_sphere, cell, source_point, tally_surface
# Lines: 7 lines of code
# Complexity: beginner
Test: ls onboarding/examples/ shows ≥ 5 annotated files.
Subtask 0.2.2: In build_index.py, ingest only these examples (chunk by full file).
Test: Run python build_index.py; query vectorstore for "sphere" → returns sphere/input.py.
Deliverable: RAG system that retrieves complete, runnable examples (not fragments).
Phase 1: Onboarding Conversational Flow (3 days)
Task 1.1: Design 7-Step Tutor Prompt
Subtask 1.1.1: In onboarding/prompts.py, write TUTOR_SYSTEM_PROMPT that encodes the curriculum:
Python
Copy
"""
You are MCDC-Tutor. Guide the user through the 7-step simulation workflow.
At each step:
1. Ask the user what they want to define (e.g., "Which material?")
2. Search examples with search_docs() to show a relevant pattern
3. Explain the function signature and required parameters
4. Ask if they want to: (A) see another example, (B) define it now, (C) skip
Only proceed to the next step when the user confirms the current step is complete.
"""
Test: Print prompt; verify it mentions all 7 steps.
Task 1.2: Build "Explain-Example-Define" Loop
Subtask 1.2.1: In onboarding/tutor.py, write explain_concept(step_name, user_goal) function that:
Calls search_docs(f"{step_name} {user_goal}")
Returns the top example plus a plain‑English explanation of the function signature.
Test: explain_concept("material", "water") returns mcdc.material() example and parameters.
Subtask 1.2.2: Create interactive_onboarding() loop that:
Iterates through steps = ["material", "surface", "geometry", "source", "tally", "settings", "run"]
At each step, asks user: "What do you want to define for {step}?"
Calls explain_concept() and prints example
Prompts: "(1) Show another example (2) Define it yourself (3) Skip step"
If user picks (2), does not generate code—instead opens editor with a template comment.
Test: Run interactive_onboarding(); simulate keystrokes 1→2→3; verify loop completes in 7 iterations.
Deliverable: Text-based tutor that walks the user through workflow, showing relevant examples at each step.
Phase 2: Guided Script Builder (3 days)
Task 2.1: Template-Based Code Generation
Subtask 2.1.1: For each of the 7 steps, create a Jinja2 template in onboarding/templates/:
jinja
Copy
{# material_template.j2 #}
# {{ user_desc }}
{{ name }} = mcdc.material(
    composition={{ composition }},
    density={{ density }}
)
Test: render_template("material", name="m_water", composition="{'H':2,'O':1}", density=1.0) produces valid line.
Subtask 2.1.2: In onboarding/builder.py, write generate_step(step, params) that:
Picks correct template
Fills in params
Returns code string + explanation.
Test: Input {"step": "material", "name": "fuel", "composition": "{'U':1}"} → output contains mcdc.material('fuel', ...).
Task 2.2: Integrate Builder into Tutor Loop
Subtask 2.2.1: Add option (4) "Generate it for me" to the interactive prompt.
Subtask 2.2.2: If user picks (4), ask 2–3 clarifying questions (e.g., "What density?"), then call generate_step() and show the code.
Subtask 2.2.3: Always append a comment: # Generated by MCDC-Tutor; review before running.
Test: Simulate full run: user picks (4) for material → agent asks density → generates line → line ends with comment.
Deliverable: Optional generation per step, with human-in-the-loop review.
Phase 3: Dependency Guardrails (2 days)
Task 3.1: Real-Time Dependency Checker
Subtask 3.1.1: Enhance ScriptBuilder with verify_step(step_name) method that:
If step_name == "geometry" → asserts len(self.entities["material"]) > 0 and len(self.entities["surface"]) > 0
If step_name == "source" → asserts at least one geometry cell exists.
Test: Call verify_step("geometry") before defining any material → raises ValueError.
Task 3.2: Auto-Detect & Suggest Missing Steps
Subtask 3.2.1: In tutor loop, before moving to next step, call verify_step(next_step). If it fails, print:
"⚠️ You need at least one material before defining a cell. Let's go back."
Test: User tries to skip from surface to source without geometry → tutor loops back.
Phase 4: RAG Enhancement for Onboarding (2 days)
Task 4.1: Tag Examples by Workflow Step
Subtask 4.1.1: For each example in onboarding/examples/, add metadata to ChromaDB:
Python
Copy
metadata = {
    "step": "material",
    "concept": "composition",
    "complexity": "beginner",
    "lines_of_code": 7
}
Test: Query vectorstore.get(where={"step": "material"}) returns only material examples.
Task 4.2: Smart Example Selection
Subtask 4.2.1: Modify search_docs() to filter by current step:
search_docs(query, filter={"step": current_step}).
Test: During "surface" step, search returns no material examples.
Phase 5: Minimal Validation & Output (2 days)
Task 5.1: Syntax Check on Full Script
Subtask 5.1.1: At end of onboarding, assemble all generated lines.
Subtask 5.1.2: Run ruff check --quiet on the assembled script.
Subtask 5.1.3: If fails, point user to the line number and suggest re‑doing that step.
Test: Deliberately generate invalid syntax (e.g., missing comma); validator catches it.
Task 5.2: Export Final Script
Subtask 5.2.1: Write save_script(filename) that writes builder.get_script() + # onboarding_complete timestamp.
Test: save_script("my_sim.py"); file exists and is runnable python my_sim.py (dry-run mode).
Phase 6: Evaluation & Iteration (2 days)
Task 6.1: Onboarding Success Metrics
Subtask 6.1.1: Track per session:
Steps completed
Times user asked for "another example"
Times user self‑defined vs. generated
Final script pass/fail
Test: Run 3 onboarding sessions; logs/onboarding_stats.json contains counts.
Task 6.2: User Walkthrough (The Real Test)
Subtask 6.2.1: Invite one graduate student who has never used MCDC.
Subtask 6.2.2: Give them the README and a 10‑minute tutorial goal: "Define a water sphere with a 1 MeV source."
Subtask 6.2.3: Observe where they get stuck; log every question they ask the tutor.
Test: Student completes workflow in < 15 minutes without asking you for help.
Deliverable: If student succeeds, onboarding is done. If they fail at step X, add 2 examples for step X and repeat.